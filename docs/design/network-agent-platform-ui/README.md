# Network Agent Platform UI Design Reference

Original static design export imported from:

```text
/Users/jasonmercer/Downloads/Network agent platform UI.zip
```

Imported files:

- `Network Console.dc.html`: operations console visual directions.
- `Network Settings.dc.html`: settings and administration surface.
- `support.js`: design-export support runtime.
- `.thumbnail`: design thumbnail from the export.
- `uploads/README.md`: source text included with the export.

The production implementation starts in `apps/web`. The static files in this directory are preserved as design evidence and should not be treated as the maintained application.
